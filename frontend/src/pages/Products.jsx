import React from 'react';

const Products = () => {
    return (
        <div className="p-8">
            <h2 className="text-2xl font-bold text-slate-800">Products</h2>
            <p className="text-gray-600 mt-2">Manage product catalog (Admin only).</p>
        </div>
    );
};

export default Products;
